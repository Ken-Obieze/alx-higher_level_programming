#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    new_square = []
    for x in matrix:
        new_square.append([])
        for y in matrix[i]:
            new_square[i].append(y ** 2)
        i = i + 1
    return (new_square)
